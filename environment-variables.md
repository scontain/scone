This file defines the environment variables that we use in this repo. 

This includes the following environment variables - which are set with the help of `tplenv`:

1. The SCONE version is stored in environment variable ${SCONE_VERSION}
   Right now this is 7.0.0-alpha.1: 
2. `$REGISTRY` - the name of the registry. By default, we set this to `registry.scontain.com`.
